# ==========================================
# NovelTrans Environment Configuration Template
# Copy this file to .env and fill in actual values
# ==========================================

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=novel_translator
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT (at least 32 characters)
JWT_SECRET=your_jwt_secret_min_32_chars

# Mail (QQ Mail example)
MAIL_USERNAME=your_email@qq.com
MAIL_PASSWORD=your_smtp_authorization_code

# Internal service-to-service authentication (Java Backend ↔ Python Translation Service)
# Must be a strong random string, shared by both services
TRANSLATE_SERVICE_API_KEY=your_shared_secret_min_32_chars

# MTranServer — independent translation engine (direct HTTP connection from backend)
MTRAN_HOST=localhost
MTRAN_PORT=8989
MTRAN_API_KEY=your_mtran_secret_min_32_chars
# Mock mode: true = return simulated responses for dev/load-testing, false = connect to real MTranServer
MTRAN_MOCK=false

# LLM Translation API (DashScope / OpenAI compatible)
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=

# Embedding API (RAG Translation Memory)
# Option A: OpenAI-compatible API (recommended, supports DashScope, SiliconFlow, Zhipu, etc.)
EMBEDDING_PROVIDER=openai
EMBEDDING_OPENAI_API_KEY=sk-your_key
EMBEDDING_OPENAI_BASE_URL=https://api.openai.com
EMBEDDING_OPENAI_MODEL=text-embedding-3-small
# DashScope example:
# EMBEDDING_OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# EMBEDDING_OPENAI_MODEL=text-embedding-v4
# Option B: Ollama local self-hosted (requires NVIDIA GPU)
# EMBEDDING_PROVIDER=ollama
# EMBEDDING_OLLAMA_BASE_URL=http://localhost:11434
# EMBEDDING_OLLAMA_MODEL=bge-m3

# Translation OpenAI API (optional, direct OpenAI fallback)
TRANSLATION_OPENAI_API_KEY=

# Translation Service Thread Pool (optional, default 2)
TRANSLATE_MAX_WORKERS=2

# Stripe Payments (Test mode)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_test_your_webhook_secret
STRIPE_SUCCESS_URL=http://localhost:7341/subscription/result
STRIPE_CANCEL_URL=http://localhost:7341/subscription/result?canceled=true
STRIPE_PRO_MONTHLY_PRICE_ID=price_pro_monthly
STRIPE_PRO_YEARLY_PRICE_ID=price_pro_yearly
STRIPE_MAX_MONTHLY_PRICE_ID=price_max_monthly
STRIPE_MAX_YEARLY_PRICE_ID=price_max_yearly

# Stripe Account ID prefix (for webhook stress-testing to generate event IDs)
STRIPE_ACCOUNT_PREFIX=YOUR_PREFIX
